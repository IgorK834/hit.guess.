/** @type {import('eslint').Linter.FlatConfig[]} */
const nextConfig = require("eslint-config-next");

// eslint-config-next already exports a flat-config array for Next.js.
module.exports = nextConfig;

